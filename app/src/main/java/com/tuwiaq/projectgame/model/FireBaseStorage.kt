package com.tuwiaq.projectgame.model

import android.app.ProgressDialog
import android.content.Context
import android.net.Uri
import android.util.Log
/*
import com.google.firebase.storage.FirebaseStorage

class FireBaseStorage {
    private val Tag = "FireBaseStorage"
         val mStorage = FirebaseStorage.getInstance().reference
private lateinit var mProgressDialog:ProgressDialog
        fun uploadImage(mContext: Context, ImageU:Uri){
            mProgressDialog = ProgressDialog(mContext)
            mProgressDialog.setMessage("Please wite, image being Dowunloading...")
              val uploadTask =  mStorage.child("users/profilePic.png").putFile(ImageU)
            uploadTask.addOnSuccessListener {
                Log.e(Tag,"Image uploa")
            }.addOnFailureListener {
                Log.e(Tag,"Image uploa fail ${it.printStackTrace()}")

            }
        }
}*/
