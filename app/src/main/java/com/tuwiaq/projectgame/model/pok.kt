package com.tuwiaq.projectgame.model

import android.content.Context
import android.content.SharedPreferences
import android.preference.PreferenceManager
import java.util.*

/*
object pok {

    val prefs =
        PreferenceManager.getDefaultSharedPreferences(ApplicationProvider.getApplicationContext<Context>())
    val idHasBeenGenerated = prefs.getBoolean("idgenerated", false)

    if (!idHasBeenGenerated) {
        val uuid = UUID.randomUUID().toString()

//do your thing with PreferenceConnector
        val editor: SharedPreferences.Editor = prefs.edit()
        editor.putBoolean("idgenerated", true)
        editor.commit()
    } else {
        //Do nothing ID has already been generated
    }
}*/
