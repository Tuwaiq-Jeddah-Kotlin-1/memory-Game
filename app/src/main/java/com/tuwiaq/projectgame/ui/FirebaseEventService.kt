package com.tuwiaq.projectgame.ui

/*
import android.util.Log
import com.google.firebase.firestore.FirebaseFirestore
import com.tuwiaq.projectgame.data.ImageUp
import kotlinx.coroutines.tasks.await

object FirebaseEventService {
    private const val TAG = "FirebaseEventService"

    private val db by lazy { FirebaseFirestore.getInstance() }

    //get event by Id
     fun getImageData(ImageId: String): ImageUp? {
        return try {
            db.collection("events")
                .document(ImageId).get().toImage
        } catch (e: Exception) {
            Log.e(TAG, "Error getting the event details", e)

            null
        }
    }
}*/
